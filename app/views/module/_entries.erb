<% @entries.each do |entry| %>
  <article class="entriesItem">
    <h1 class="title"><a href="/entry/<%= entry.publish_date.strftime("%Y/%m/%d/#{entry.id}") %>"><%= entry.title %></a></h1>
    <!-- 詳細 -->
    <ul class="detail">
      <li class="item date"><%= entry.publish_date.strftime("%Y-%m-%d") %></li>
      <li class="item author">sawa-zen</li>
    </ul>
    <div class="content">
      <!-- アイキャッチ -->
      <div class="eyeCatch">
        <img src="/assets/img/dummy_eye_catch.png" alt="">
      </div>
      <!-- ヘッドライン -->
      <div class="headline">
        最近 MinecraftPE も アップデートでかなりPC版に近づいてきたので RaspberryPi でサーバを建ててみました。少し躓いたのでまとめておきます。最近 MinecraftPE も アップデートでかなりPC版に近づいてきたので...
      </div>
    </div>
  </article>
<% end %>
<%= will_paginate(
  @entries,
  :inner_window => 1,
  :outer_window => 1,
  :previous_label => '前へ',
  :next_label => '次へ')
%>
