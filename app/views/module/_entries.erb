<% (1..9).each do |entry| %>
  <article class="entriesItem">
  </article>
<% end %>
<%= will_paginate(
  @entries,
  :inner_window => 1,
  :outer_window => 1,
  :previous_label => '前へ',
  :next_label => '次へ')
%>
